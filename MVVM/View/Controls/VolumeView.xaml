<UserControl x:Class="MediaPortal.MVVM.View.Controls.VolumeView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaPortal.MVVM.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
    <Grid>
        <!-- Button for Volume -->
        <Button x:Name="VolumeButton" Style="{StaticResource MediaControlIconButtonStyle}" Content="{StaticResource VolumeIcon}" ToolTip="Volume" Click="VolumeButton_Click" VerticalAlignment="Bottom" HorizontalAlignment="Center"/>

        <!-- Slider for Volume control -->
        <Slider x:Name="VolumeSlider" Orientation="Vertical" Visibility="Collapsed" Width="30" Height="100"
            VerticalAlignment="Top" HorizontalAlignment="Center" Margin="0,-100,-10,0"
            Value="{Binding Volume, RelativeSource={RelativeSource AncestorType=UserControl}, Mode=TwoWay}"
            Minimum="0" Maximum="1" TickFrequency="0.1"/>
    </Grid>
</UserControl>
