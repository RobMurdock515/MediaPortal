<UserControl x:Class="MediaPortal.MVVM.View.Controls.PlayPauseView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MediaPortal.MVVM.View.Controls"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="50">
    <Grid>
            <!-- Button for Play/Pause -->
            <Button x:Name="PlayButton" 
                    Style="{StaticResource MediaControlIconButtonStyle}" 
                    Content="{StaticResource PlayIcon}" 
                    ToolTip="Play/Pause" 
                    Click="PlayButton_Click" />
    </Grid>
</UserControl>
